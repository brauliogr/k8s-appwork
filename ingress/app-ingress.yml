apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
  name: appwork-web
spec:
  rules:
  - host: ec2-3-215-16-127.compute-1.amazonaws.com 
    http:
      paths:
      - backend:
          service:
            name: appwork-web
            port:
              number: 3000
        path: /
        pathType: Prefix
